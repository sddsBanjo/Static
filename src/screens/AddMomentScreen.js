import { View, Text } from 'react-native';

export default function AddMomentScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>➕ Novo Momento</Text>
    </View>
  );
}